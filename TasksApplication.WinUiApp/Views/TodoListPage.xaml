<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="TasksApplication.WinUiAPP.Views.TodoListPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TasksApplication.WinUiAPP.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid x:Name="MainGrid" Padding="20">
        <StackPanel Spacing="12">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Menu" Click="MenuButton_Click" />
                <TextBlock Text="Task List" 
               FontSize="16"
               Foreground="Red"/>
            </StackPanel>
            <!-- Input Titolo -->
            <StackPanel Orientation="Horizontal" Spacing="8">
                <TextBlock Text="Titolo: " FontSize="16"/>
                <TextBox PlaceholderText="Inserisci titolo"
                 Text="{Binding Title, Mode=TwoWay}" /> 
            </StackPanel>

            <!-- Input Descrizione -->
            <StackPanel Orientation="Horizontal" Spacing="8">
                <TextBlock Text="Descrizione: " FontSize="16"/>
                <TextBox PlaceholderText="Inserisci descrizione"
                 Text="{Binding Description, Mode=TwoWay}" 
                 InputScope="Text" />
            </StackPanel>

            <StackPanel Orientation="Horizontal" Spacing="8">
                <TextBlock Text="Inizio: " FontSize="16"/>
                <DatePicker 
                 Date="{Binding StartDate, Mode=TwoWay}" />
            </StackPanel>

            <StackPanel Orientation="Horizontal" Spacing="8">
                <TextBlock Text="Scadenza: " FontSize="16"/>
                <DatePicker 
                 Date="{Binding DueDate, Mode=TwoWay}" />
            </StackPanel>
            <StackPanel>
            <Button Content="Inserisci"
             Command="{Binding AddNewTodoCommand}" />

                <Button Content="Elimina"
             Command="{Binding DeleteTodoCommand}" /> 
            </StackPanel>

            <!-- Lista clienti -->
            <ListView x:Name="TodoListView" ItemsSource="{Binding Todos}" 
                      SelectedItem="{Binding SelectedTodo, Mode=TwoWay}"
                      Height="400">
                <ListView.Header>
                    <TextBlock Text="My Todo List" 
                   FontSize="24" 
                   FontWeight="Bold" 
                   Margin="10"/>
                </ListView.Header>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <TextBlock Text="{Binding Title}" />
                            <TextBlock Text=" - " />
                            <TextBlock Text="{Binding StartDate}" />
                            <TextBlock Text="{Binding DueDate}" />
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <ContentDialog x:Name="TodoDialog"
                       Title="TodoTask"
                       PrimaryButtonText="Save"
                       CloseButtonText="Cancel"
                       DefaultButton="Primary">
                <StackPanel Spacing="8">
                    <TextBox x:Name="TitleBox" PlaceholderText="Enter title"/>
                    <TextBox x:Name="DescriptionBox" PlaceholderText="Enter description"/>
                    <DatePicker x:Name="StartDatePicker"/>
                    <DatePicker x:Name="DueDatePicker"/>
                </StackPanel>
            </ContentDialog>
        </StackPanel>
    </Grid>
</Page>
